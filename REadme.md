## Local Installation

- Install pipenv
- Install dependencies
    - `pipenv shell`
    - `pipenv install -r requirements`
- Start the app
    - `python3 main.py`

## Configuration
- Update the settings.toml file
